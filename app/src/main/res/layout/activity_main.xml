<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" >

    <ListView
        android:id="@+id/list_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:divider="#0000"
        android:listSelector="@drawable/list_selector"
        android:layout_marginTop="0dp">
    </ListView>

    <RelativeLayout
        android:id="@+id/layout_main_1"
        android:layout_width="match_parent"
        android:layout_below="@id/list_view"
        android:layout_height="80dp">

        <Button
            android:id="@+id/new_button_main"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:background="#ff209e85"
            android:textSize="20sp"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:text="@string/new_activity_main"/>

    </RelativeLayout>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_below="@id/layout_main_1"
        android:layout_height="80dp">

        <Button
            android:id="@+id/delete_button_main"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:background="#ff209e85"
            android:textSize="20sp"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:text="@string/delete_activity_main"/>

    </RelativeLayout>


</RelativeLayout>